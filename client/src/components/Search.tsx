import React from 'react';

const Search = () => {
  return (
    <div>
      <input
        type="text"
        name="search"
        placeholder="Find something for yourself"
      />
    </div>
  );
};

export default Search;
